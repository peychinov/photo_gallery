%h1 Listing categories

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @categories.each do |category|
    %tr
      %td= category.name
      %td= link_to 'Show', category, class: 'btn'
      %td= link_to 'Edit', edit_category_path(category), class: 'btn'
      %td= link_to 'Destroy', category, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn'

%br

= link_to 'New Category', new_category_path, class: 'btn'
